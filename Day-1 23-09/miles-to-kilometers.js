// Example 1: to Miles to KM

const miles =1.3670162000000001;

// conversion factor
const factor = 0.621371

// calculate miles
const km = miles/factor;
console.log(`${miles} miles is equal to ${km} km.`);